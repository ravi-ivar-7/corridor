{
  "version": 2,
  "buildCommand": "npm install && cd server/web && npm install && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "server/web/.next",
  "framework": "nextjs"
}
